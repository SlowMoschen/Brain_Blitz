import * as users from './users.schemas';
import * as tokens from './tokens.schema';
import * as quizzes from './quizzes.schemas';
import * as achievements from './achievements.schemas';

const schema = {
  users,
  tokens,
  quizzes,
  achievements,
};

export default schema;

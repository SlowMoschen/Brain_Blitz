import { Box, Typography } from "@mui/material";

export default function Footer() {
    return (
        <Box sx={{ bgcolor: 'background.secondary', mx: 1, borderRadius: 1}}>
            <Typography variant="h6">Footer</Typography>
        </Box>
    )
}